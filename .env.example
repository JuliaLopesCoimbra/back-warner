# Copie para `back/.env` e preencha (não commite segredos).
DATABASE_URL=postgresql://USER:SENHA@HOST:PORT/NOME_DO_BANCO

# Assinatura do embaralhamento das alternativas no quiz (troque em produção).
APP_SECRET=uma-chave-longa-e-aleatoria
